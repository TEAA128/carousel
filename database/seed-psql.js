const faker = require('faker');

const generatedData = [];

function generatePlace (numberOfTitles) {
  for (let i = 0; i < numberOfTitles; i++) {
    const obj = {
      title:
    };

    generatedData.push(obj);
  }

}

generatePlace(5);
console.log(generatedData);